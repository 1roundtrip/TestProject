# 数据库导入顺序说明

本文档详细列出了智慧煤矿ERP管理系统数据库的完整导入顺序。

## 导入顺序总览

数据库导入必须按照以下顺序执行，以确保表结构、外键约束和权限配置的正确性。

---

## 第一步：创建数据库

```sql
CREATE DATABASE IF NOT EXISTS coal_erp CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE coal_erp;
```

---

## 第二步：导入基础表结构

**文件路径**: `coal-erp-business/src/main/resources/db/schema.sql`

**说明**: 包含系统基础表结构（用户表、角色表、菜单表、部门表等）

---

## 第三步：导入采购模块

### 3.1 采购模块表结构（按顺序导入）

**文件路径**: `coal-erp-business/src/main/resources/db/purchase/`

1. `purchase_contract_schema.sql` - 采购合同表结构
2. `purchase_integration_schema.sql` - 采购集成表结构
3. `purchase_order_schema.sql` - 采购订单表结构
4. `purchase_payment_schema.sql` - 采购付款表结构
5. `purchase_plan_schema.sql` - 采购计划表结构
6. `purchase_quality_schema.sql` - 采购质检表结构
7. `purchase_receiving_schema.sql` - 采购收货表结构
8. `purchase_report_schema.sql` - 采购报表表结构
9. `purchase_requisition_schema.sql` - 采购申请表结构
10. `purchase_return_schema.sql` - 采购退货表结构
11. `purchase_supplier_schema.sql` - 供应商表结构

### 3.2 采购模块权限菜单

**文件路径**: `coal-erp-business/src/main/resources/db/purchase_permission_menu.sql`

**说明**: 采购模块的菜单权限配置

---

## 第四步：导入库存模块

### 4.1 库存模块表结构

**文件路径**: `coal-erp-business/src/main/resources/db/inventory/inventory_schema.sql`

**说明**: 库存管理相关表结构

### 4.2 库存模块权限菜单

**文件路径**: `coal-erp-business/src/main/resources/db/inventory/inventory_permission_menu.sql`

**说明**: 库存模块的菜单权限配置

---

## 第五步：导入维修管理模块

### 5.1 维修管理模块表结构

**文件路径**: `coal-erp-business/src/main/resources/db/maintenance/maintenance_schema.sql`

**说明**: 维修管理相关表结构

### 5.2 维修管理模块权限菜单

**文件路径**: `coal-erp-business/src/main/resources/db/maintenance/maintenance_permission_menu.sql`

**说明**: 维修管理模块的菜单权限配置

---

## 第六步：导入预警模块

### 6.1 预警模块表结构

**文件路径**: `coal-erp-business/src/main/resources/db/warning/warning_schema.sql`

**说明**: 预警管理相关表结构

### 6.2 预警模块权限菜单

**文件路径**: `coal-erp-business/src/main/resources/db/warning/warning_permission_menu.sql`

**说明**: 预警模块的菜单权限配置

---

## 第七步：导入资产模块

### 7.1 资产模块表结构

**文件路径**: `coal-erp-business/src/main/resources/db/asset_management_schema.sql`

**说明**: 资产管理相关表结构

### 7.2 资产模块权限菜单

**文件路径**: `coal-erp-business/src/main/resources/db/asset_permission_menu.sql`

**说明**: 资产模块的菜单权限配置

---

## 第八步：配置角色和权限

### 8.1 配置模块负责人角色和权限

**文件路径**: `coal-erp-business/src/main/resources/db/setup_module_managers.sql`

**说明**: 
- 创建5个模块负责人角色（资产中心、采购中心、维修管理、库存中心、预警中心）
- 配置每个角色的权限（本模块全部权限，其他模块只读权限）
- 确保admin和admin2为超级管理员

### 8.2 创建模块负责人用户

**文件路径**: `coal-erp-business/src/main/resources/db/create_module_manager_users.sql`

**说明**: 创建5个模块负责人用户账户（密码均为123456）

### 8.3 修复系统管理权限

**文件路径**: `coal-erp-business/src/main/resources/db/diagnose_and_fix_system_permissions.sql`

**说明**: 确保系统管理模块仅对超级管理员可见，其他角色无权限访问

---

## 完整导入顺序清单

| 序号 | 文件路径 | 说明 |
|------|---------|------|
| 1 | 创建数据库 | `CREATE DATABASE coal_erp` |
| 2 | `db/schema.sql` | 基础表结构 |
| 3 | `db/purchase/purchase_contract_schema.sql` | 采购合同表 |
| 4 | `db/purchase/purchase_integration_schema.sql` | 采购集成表 |
| 5 | `db/purchase/purchase_order_schema.sql` | 采购订单表 |
| 6 | `db/purchase/purchase_payment_schema.sql` | 采购付款表 |
| 7 | `db/purchase/purchase_plan_schema.sql` | 采购计划表 |
| 8 | `db/purchase/purchase_quality_schema.sql` | 采购质检表 |
| 9 | `db/purchase/purchase_receiving_schema.sql` | 采购收货表 |
| 10 | `db/purchase/purchase_report_schema.sql` | 采购报表表 |
| 11 | `db/purchase/purchase_requisition_schema.sql` | 采购申请表 |
| 12 | `db/purchase/purchase_return_schema.sql` | 采购退货表 |
| 13 | `db/purchase/purchase_supplier_schema.sql` | 供应商表 |
| 14 | `db/purchase_permission_menu.sql` | 采购模块权限菜单 |
| 15 | `db/inventory/inventory_schema.sql` | 库存模块表结构 |
| 16 | `db/inventory/inventory_permission_menu.sql` | 库存模块权限菜单 |
| 17 | `db/maintenance/maintenance_schema.sql` | 维修管理模块表结构 |
| 18 | `db/maintenance/maintenance_permission_menu.sql` | 维修管理模块权限菜单 |
| 19 | `db/warning/warning_schema.sql` | 预警模块表结构 |
| 20 | `db/warning/warning_permission_menu.sql` | 预警模块权限菜单 |
| 21 | `db/asset_management_schema.sql` | 资产模块表结构 |
| 22 | `db/asset_permission_menu.sql` | 资产模块权限菜单 |
| 23 | `db/setup_module_managers.sql` | 配置模块负责人角色和权限 |
| 24 | `db/create_module_manager_users.sql` | 创建模块负责人用户 |
| 25 | `db/diagnose_and_fix_system_permissions.sql` | 修复系统管理权限 |

---

## 快速导入方法

### 方法一：使用一键导入脚本（推荐）

**Windows系统**:
```bash
一键导入数据库.bat
```

**Linux系统**:
```bash
chmod +x 一键导入数据库.sh
./一键导入数据库.sh
```

### 方法二：手动导入

按照上述顺序，使用MySQL客户端依次执行每个SQL文件：

```bash
mysql -u root -p coal_erp < schema.sql
mysql -u root -p coal_erp < purchase/purchase_contract_schema.sql
# ... 依次执行其他文件
```

---

## 注意事项

1. **必须按顺序导入**: 由于存在外键依赖关系，必须严格按照上述顺序导入
2. **字符集设置**: 确保数据库使用 `utf8mb4` 字符集和 `utf8mb4_unicode_ci` 排序规则
3. **权限配置**: 最后三个权限配置文件必须按顺序执行，确保角色和用户正确关联
4. **错误处理**: 如果导入过程中出现错误，请检查：
   - 数据库连接是否正常
   - 前序表结构是否已正确导入
   - SQL文件是否存在语法错误

---

## 导入完成后的验证

导入完成后，可以使用以下SQL验证：

```sql
-- 检查表数量
SELECT COUNT(*) as table_count FROM information_schema.tables 
WHERE table_schema = 'coal_erp';

-- 检查用户
SELECT username, nick_name, status FROM sys_user;

-- 检查角色
SELECT role_name, role_key, status FROM sys_role;

-- 检查菜单
SELECT COUNT(*) as menu_count FROM sys_menu;
```

---

## 默认账户信息

### 超级管理员账户
- **用户名**: `admin` 或 `admin2`
- **密码**: `admin123`
- **权限**: 系统所有权限

### 模块负责人账户（密码均为 `123456`）
- `asset_manager` - 资产中心负责人
- `purchase_manager` - 采购中心负责人
- `maintenance_manager` - 维修管理负责人
- `inventory_manager` - 库存中心负责人
- `warning_manager` - 预警中心负责人

---

**文档版本**: v1.0  
**最后更新**: 2024年

